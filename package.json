{
  "name": "excelsior",
  "version": "2.1.0",
  "description": "Excelsior! — Comic Book Tracker",
  "private": true,
  "scripts": {
    "dev": "bash dev.sh",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
