# Leave unset to use the same origin (/api) — works with the Vite dev proxy and
# when the backend serves the built frontend. Set when the API is on another host.
# VITE_API_URL=https://your-api.up.railway.app/api
