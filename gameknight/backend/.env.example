PORT=4000
# Comma-separated usernames that get admin rights (create + settle fixtures).
# If empty, the first account ever registered becomes admin.
ADMIN_USERS=admin
FRONTEND_URL=http://localhost:5174
# Set to false to skip seeding demo fixtures on an empty database
SEED_DEMO=true
